use sycamore::prelude::*;
use sycamore::futures::spawn_local_scoped;
use wasm_bindgen::prelude::*;

mod app;

fn main() {
  console_error_panic_hook::set_once(); // for trouble shooting
  sycamore::render(app::App);
}